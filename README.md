## Spring boot microservice with Kotlin

### step one
open a command window with: 
`gradle build --continuous`

### step two
open a new command window with: 
`gradle bootRun`

### Step three
access with:
`http://localhost:8080`